
# --- Sweep / withdraw mode ---
# SWEEP_MODE=true -> engine STOPS buyback/burn, sells all held tokens to SOL,
# and sends all SOL to SWEEP_DESTINATION. Set back to false to resume normal operation.
SWEEP_MODE=false
SWEEP_DESTINATION=
